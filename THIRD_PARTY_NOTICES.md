# 第三方组件与许可证提示（发行时请随包提供）

> **说明**：以下内容为便于合规的摘要，**不构成法律意见**。公开发布、商用或再分发二进制前，请自行核对各组件**当时版本**的官方许可证全文，必要时咨询专业律师。

---

## 本仓库「MIT」指什么、不指什么

- **`LICENSE`（MIT）** 仅适用于本仓库中的 **Mp4ToFrame 自研源代码与文档**（主要为 C# / XAML 等）。
- **不包括**：用户或你另行提供的 **`ffmpeg.exe`**、**Python / rembg**、**rembg 下载的模型文件**、以及 **.NET 运行时**（若自包含发布）等；这些部件各有自己的许可证，由提供方承担相应义务。

---

## 本程序不内置、由用户或发行包提供的组件

| 组件 | 说明 |
|------|------|
| **FFmpeg（一般）** | 以可执行文件或库形式再分发时，须遵守你所使用**具体构建版本**的条款。请参阅 [FFmpeg 法律与许可证 FAQ](https://ffmpeg.org/legal.html)。**FFmpeg** 名称相关商标与使用规范亦以官方说明为准。 |
| **rembg** | 以实际安装的包版本为准，常见为 MIT，见 [rembg 仓库](https://github.com/danielgatis/rembg)。 |
| **rembg 所下载的模型权重** | 各模型（如 U²-Net、ISNet、BiRefNet 等）可能有**单独许可**（学术/非商用/商用限制等）。**商用或再分发含模型的产物前**，请查阅 rembg 与各模型官方说明。 |
| **.NET 运行时** | 自包含发布时捆绑的运行时遵循 [Microsoft .NET 许可](https://dotnet.microsoft.com/license)。 |

本仓库中的 Mp4ToFrame **源码**不嵌入上述二进制；仅通过**启动外部进程**调用用户环境中的 `ffmpeg` 与 `rembg`（若用户启用相应功能）。

---

## 程序内「一键下载」涉及的 FFmpeg 构建（务必单独声明）

若用户使用界面 **「下载便携版到程序目录」**，程序会从以下来源拉取 zip 并解压出 `ffmpeg.exe`：

| 来源 | 默认地址（可能随上游变更） | 许可证提示 |
|------|---------------------------|------------|
| **BtbN FFmpeg-Builds** | `https://github.com/BtbN/FFmpeg-Builds/releases/…/ffmpeg-master-latest-win64-gpl.zip` | 当前默认包名为 **`-gpl`** 构建，整体按 **GPL** 等要求分发 FFmpeg；**若你将下载得到的 `ffmpeg.exe` 与 Mp4ToFrame 一并提供给他人**，通常须同时满足 GPL 对再分发的要求（例如附带或指向相应许可证与源码/构建说明，具体要求以 GPL 及 FFmpeg 官方说明为准）。 |

若用户使用 **「使用 winget 安装」**，实际安装的是 **winget 源中的软件包**（例如常见为 **Gyan.FFmpeg** 等），**许可证以该包及对应构建维护方为准**；与本仓库 MIT **无关**。

---

## 网络访问与隐私（本程序行为摘要）

- **下载便携 FFmpeg**：会向 **GitHub**（及重定向后的发布存储）发起 HTTPS 请求；可能产生访问日志，以 GitHub 隐私政策为准。
- **rembg / 模型**：由用户本机上的 **rembg** 在首次使用等场景自行联网下载模型，请求目标与日志以 **rembg 及其模型托管方** 为准。
- **本程序**：**未内置**向作者服务器上报使用情况、崩溃统计等遥测逻辑；**用户设置**保存在本机 `%AppData%\Mp4ToFrame\settings.json`，不上传。
- **用户内容**：视频、图片、工作区路径等均由用户自备并在本地处理；**内容版权与是否允许处理**由用户自行负责。

---

## 用户生成内容与责任

对工作区中的 **MP4、序列帧、抠图结果** 等，**著作权与授权**由用户自行保证。本工具仅提供技术能力，**不就用户素材的合法性作任何担保**。

---

## 再分发检查清单（给打包发布的人）

- [ ] 随包附带 **`LICENSE`**（MIT）与 **`THIRD_PARTY_NOTICES.md`**（本文件）。  
- [ ] 若包内含有 **`ffmpeg.exe`**（含程序内下载后一并打包）：确认已满足该构建的 **GPL/LGPL 等** 义务，并保留/附上相应许可与来源说明。  
- [ ] 若提供自包含 exe：注意 **.NET 运行时** 的再分发条款。  
- [ ] 向最终用户说明 **rembg / 模型** 为可选、需单独安装及可能涉及的 **商用许可**。  
